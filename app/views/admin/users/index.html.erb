<div class="container my-5">
  <h1 class="mb-4">会員管理一覧</h1>
  <hr>

  <% if @users.present? %>
    <table class="table table-hover table-striped text-center">
      <thead class="thead-dark">
        <tr>
          <th>ID</th>
          <th>ユーザー名</th>
          <th>メールアドレス</th>
          <th class="text-sm">コミュニティ<br>参加数</th>
          <th>投稿数</th>
          <th>コメント数</th>
          <th>ステータス</th>
          <th>詳細</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.community_members.count rescue 'N/A' %>
            <td><%= user.posts.count rescue 'N/A' %></td>
            <td><%= user.comments.count rescue 'N/A' %></td>
            <td>
              <% if user.is_active %>
                <span class="badge badge-success">有効</span>
              <% else %>
                <span class="badge badge-danger">停止中 (退会済)</span>
              <% end %>
            </td>
            <td><%= link_to "詳細", admin_user_path(user), class: "btn btn-sm btn-info" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="d-flex justify-content-center">
      <%= paginate @users %>
    </div>

  <% else %>
    <%# ユーザーが1人も存在しない場合に表示されるメッセージ %>
    <div class="alert alert-info text-center" role="alert">
      <strong>現在、登録したユーザーはいません。</strong>
      <p class="mt-2 mb-0">ユーザーが登録されるまでお待ちください。</p>
    </div>
  <% end %>
</div>